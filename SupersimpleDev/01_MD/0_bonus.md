window.console
window.alert
window.console.log('window')

Javascript  is have a built in object called window. 
The window object is the global object in the browser environment.
It represents the browser window or tab and provides various properties and methods to interact with the browser.
No need to declare window object, it is automatically available in the browser environment.